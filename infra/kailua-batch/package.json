{
    "name": "kailua-batch",
    "main": "index.ts",
    "devDependencies": {
        "@types/node": "^18",
        "typescript": "^5.0.0"
    },
    "dependencies": {
        "@pulumi/aws": "^6.0.0",
        "@pulumi/awsx": "^2.0.2",
        "@pulumi/pulumi": "^3.113.0",
        "dotenv": "^16.5.0"
    },
    "scripts": {
        "build": "tsc && cd trigger-lambda && npm install && npm run build && cd ../watchdog-lambda && npm install && npm run build"
    }
}
