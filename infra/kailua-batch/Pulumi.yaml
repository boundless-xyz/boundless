name: kailua-batch
description: Scheduled AWS Batch jobs for the Kailua workload
runtime:
  name: nodejs
  options:
    packagemanager: npm
config:
  pulumi:tags:
    value:
      pulumi:template: aws-typescript
