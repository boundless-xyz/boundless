---
galaxy_info:
  author: Boundless
  description: Install and configure Bento agent service
  license: license (BSD, MIT)
  min_ansible_version: '2.9'
  platforms:
    - name: Ubuntu
      versions:
        - all
  galaxy_tags:
    - boundless
    - bento
    - systemd

# Dependencies are included conditionally in playbooks, not here
# This allows deploying Bento without dependencies to worker nodes
# dependencies:
#   - role: postgresql
#   - role: valkey
#   - role: minio
#   - role: rust
#   - role: rzup
