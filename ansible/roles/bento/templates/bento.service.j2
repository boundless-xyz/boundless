[Unit]
Description=Bento Docker Service
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory={{ bento_dir }}
EnvironmentFile={{ bento_env_file }}
ExecStart=/usr/bin/docker compose up -d
Restart=always
RestartSec=10
TimeoutStartSec=1800
TimeoutStopSec=300
