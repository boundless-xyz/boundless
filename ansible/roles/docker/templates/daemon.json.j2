{
    "iptables": false{{"," if docker_nvidia_enabled else ""}}
    {% if docker_nvidia_enabled %}
    "runtimes": {
        "nvidia": {
            "args": [],
            "path": "nvidia-container-runtime"
        }
    }
    {% endif %}
}
