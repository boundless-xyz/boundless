[package]
name = "identity"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
postcard = "1.0"
risc0-zkvm = { git = "https://github.com/risc0/risc0", branch = "release-2.1", default-features = false, features = ["std", "unstable"] }

[patch.crates-io]
sha2 = { git = "https://github.com/risc0/RustCrypto-hashes", tag = "sha2-v0.10.8-risczero.0" }
